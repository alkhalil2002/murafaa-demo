# مُرافعة — المراجعة القرائية الأولى (Read-Only Audit)

> **المصدر:** جلسة كلود كود بتاريخ 2026-07-05 على فرع `ALAFALEQ`.
> **النطاق المقروء:** `CLAUDE.md` · `README.md` · `docs/01`–`06` كاملة · تصفّح وفحص `prototype/murafaa-prototype.html` (2,879 سطراً، 586 دالة) مع التحقق من مطابقة الدوال والثوابت المذكورة في الوثائق.
> **الحالة:** بانتظار اعتماد المالك قبل كتابة أي كود.

---

## (أ) فهمي للمعمارية

**الشكل العام:** تطبيق واحد (monolith) بـ Next.js App Router + TypeScript strict على Cloud Run، بثلاث طبقات واضحة:

1. **طبقة العرض** — React بواجهة RTL عربية بالكامل، Tailwind + shadcn/ui، خطوط IBM Plex Sans Arabic (نص) وAmiri (عناوين)، وdesign tokens من ثيم النموذج (`--bench`, `--gold`, `--advocate`, `--parch`). كل النصوص عبر طبقة i18n — لا نص عربي مكتوب مباشرة في المكوّنات. الواجهة **تُخفي فقط** ولا تُعتبر طبقة أمان.
2. **طبقة الخدمة** — server actions / route handlers، وهي مكان فرض كل شيء: فحص الصلاحيات بطبقاتها الثلاث **قبل أي منطق**، التحقق من المدخلات بـ Zod، تطبيق قواعد العمل (VAT، مهلة الاعتراض، الأتعاب…)، تسجيل `AUDIT`، واستجابات موحّدة `{data, error}`.
3. **طبقة البيانات** — Prisma (المخطّط مصدر الحقيقة + migrations) → PostgreSQL على Cloud SQL بمنطقة الدمام `me-central2` + امتداد pgvector للقاعدة المعرفية. الملفات على GCS بالدمام. كل شيء داخل المملكة (PDPL).

**حولها:** Auth.js بدخول OTP (جوال/واتساب) وجلسات httpOnly · مهام خلفية عبر Cloud Tasks/Scheduler (مهل الاعتراض، مهام ما بعد الجلسة، مسيّر الرواتب وخصم السُّلف، WPS، تذكيرات، فحص تعارض التقويم) · تكاملات بنمط adapter بواجهة موحّدة وقابلة للتعطيل (ناجز، نفاذ، زاتكا، WPS) وأسرارها في Secret Manager · ثلاث بوابات: المكتب (الأساسية)، العميل، الموظف.

**أخطر مكوّن — بوابة الذكاء القانوني:** كل مخرَج قانوني (المساعد، ساحة المرافعة، تحليل القضية) يمرّ إلزامياً بمسار واحد: استرجاع من قاعدة معرفية **مغلقة** (pgvector) → توليد بـ Claude عبر Vertex AI مقيّد بالمصادر المسترجَعة فقط → **بوابة تحقق برمجية** تستخرج كل استشهاد (مادة/نظام/سابقة) وتطابقه مع مصدر فعلي، وغير المطابق يُحجَب ويُسجَّل → العرض مع روابط المصادر وإخلاء «استرشادي لا فتوى». هذه البوابة **غير موجودة في النموذج** (النموذج يستدعي Claude حيّاً داخل Claude.ai مع نصوص توضيحية كاحتياط، وضابطه الوحيد تعليمة «لا تختلق أرقاماً») — وبناؤها البرمجي الصارم شرط إطلاق.

**الصلاحيات (تفهمها كطبقة معمارية):** 6 أدوار × 13 وحدة × 4 مستويات (none/view/edit/full)، بثلاث طبقات فرض على الخادم: وصول الوحدة → منع الحقل (مثال حالي وحيد: المساعد ممنوع من `fees`) → نطاق الصفوف على القضايا (all/assigned). جداول `roles`/`permissions`/`field_permissions` قابلة للتحرير من شاشة الصلاحيات. **لا god mode**: في النموذج `can()` تُرجع `true` عند غياب دور الجلسة — هذا يُلغى، والمالك يأخذ دور «شريك» صراحة.

**ترتيب البناء:** أساس (مشروع+Auth+نموذج بيانات أساسي+صلاحيات+تدقيق) → قضايا/CRM/مهام/مواعيد + كشف التعارض → مستندات → مالية → **بوابة الذكاء** → HR → تكاملات → بوابتا العميل والموظف → Pilot. الـ MVP = المراحل 1–4.

---

## (ب) نموذج البيانات كما قرأته

**الكيان المركزي `cases`** وحوله كل شيء: عنوان ورقم، موكّل (`entity` → FK لـ `clients`)، خصم (`party` — محور كشف التعارض)، مدينة، تصنيف ناجز ثلاثي المستوى (رئيسي/فرعي/نوع)، مرحلة إجرائية (صلح → درجة أولى → استئناف → نقض → تنفيذ)، حالة، مآل (كسب/جزئي/تسوية/خسارة)، `objectionDue`، أرشفة، تقرير عميل.

**قرارات التطبيع الملزمة** (النموذج يربط بالأسماء النصية — يتحوّل كله لمفاتيح):
- `hearings`, `fee`, `timeline`, `minutes` مضمّنة اليوم داخل القضية → **جداول مستقلة** بـ FK.
- المسنَدون: **جدول وصل** `case_assignees` (cases ⇄ users، علاقة *—*).
- كل علاقة نصية (`caseTitle`, `client`, `emp`…) → `case_id`, `client_id`, `employee_id`.
- الحقول المختصرة (`n`, `st`, `t`, `d`…) → أسماء واضحة.
- حقول إلزامية لكل جدول: `id` UUID · `created_at` · `updated_at` · `created_by` · `deleted_at` (الحذف الناعم = سلة المحذوفات).

**المجموعات:**
- **تشغيل:** `clients` (فرد/شركة) · `leads` (خط أنابيب CRM، يتحوّل لقضية) · `users` (بوابة المكتب، دور من 6) · `appts` · `tasks` (كانبان + فئات + نقاط) · `templates` · `kb`.
- **مالية:** `invoices` (صافي/ضريبة/إجمالي + بنود `inv_items` + **جدول مدفوعات** — الحالة **تُشتق ولا تُخزَّن**) · `expenses` (قابلة للفوترة → إعادة فوترة) · `time_entries` (أساس أتعاب الساعة) · `trust` **بدفتر منفصل تماماً عن الإيرادات** · دفاتر: شجرة حسابات، قيود، ميزان مراجعة، أعمار ديون، إشعارات دائنة.
- **HR:** `employees` (+`emp_extra`: جنسية وسنة التحاق — للسعودة ونهاية الخدمة) · `advances` (قسط شهري يُخصم آلياً في المسيّر) · `leaves` · `emp_requests` · `payroll_runs` (أساسي + بدلات − GOSI − سُلف) · `recruit`.
- **اعتمادات وبوابات:** `proposals` (مذكرة/أتعاب/تسوية) · `client_appr` · `client_reports` · `report_approvals`.
- **نظام:** `notifs` · `activity` · `audit` · `perf_log` · `recycle` (عبر `deleted_at`).
- **مرجعية (lookups قابلة للتوسعة):** المدن، ناجز، مراحل التقاضي (5)، أنواع الجلسات (10)، حالات التنفيذ (4)، نطاقات السعودة (6)، فئات المصروفات، تصنيفات واتساب، التكاملات، الباقات.

**المال:** يُخزَّن **هللات بأعداد صحيحة** (النموذج يستخدم ريالات — يتغيّر). كل عملية مالية (فاتورة+بنود+ضريبة، قيد، حركة أمانة) داخل **DB transaction** واحدة.

**قواعد العمل المحسوبة في طبقة الخدمة** (كلها بتغطية اختبارات وحدة، تحقّقت من كل واحدة في كود النموذج):
VAT 15% (`round(net×0.15)`) · مهلة الاعتراض = تاريخ الحكم + 30 يوماً تقويمياً · 4 أنواع أتعاب (مقطوع/شهري/بالساعة = الساعات **القابلة للفوترة وغير المفوترة بعد** × السعر/نسبة من المحكوم به = `awarded×value/100`) · كشف التعارض بـ 4 حالات (خصم=عميل high، خصم=lead med، خصم=موكّل بقضية أخرى high، موكّل=خصم بقضية أخرى high) · نهاية الخدمة (نصف أجر × 5 سنوات + أجر كامل بعدها) · نطاقات السعودة والمطلوب للنسبة المستهدفة · حالة الفاتورة المشتقة (معدوم→مدفوعة→جزئياً→متأخرة→مستحقة) · خصم السُّلف (`monthly` أو `amount/months`) · إلحاح المهل · أتمتة ما بعد الجلسة (مهام تحضير، تعارض تقويم، تذكير عميل).

---

## (ج) الأسئلة المفتوحة والتناقضات

### أسئلة معمارية تحتاج قرارك قبل المرحلة 1

1. **تعدد المستأجرين (Multi-tenancy) — أهم سؤال في الوثائق كلها ولا تجيب عنه أي وثيقة.** شاشة الباقات (`PLANS`/`PMODULES`) توحي بمنتج SaaS لعدة مكاتب، لكن نموذج البيانات لا يحتوي أي `office_id`/`tenant_id`، والخطة تنتهي بـ Pilot على مكتب واحد. هل نبني من اليوم الأول بعمود `office_id` في كل جدول (وأثره على كل استعلام وفهرس ونطاق صفوف)، أم نسخة مكتب واحد والتعدد لاحقاً؟ **توصيتي: عمود `office_id` من اليوم الأول** حتى لو شغّلنا مكتباً واحداً — إضافته لاحقاً هجرة مؤلمة عبر كل النظام.
2. **توفّر Claude على Vertex AI في الدمام (`me-central2`).** الوثيقة تفترض نقطة إقليمية، والغالب أن نماذج Claude غير متاحة في هذه المنطقة تحديداً — وهو ما تلمّح له `docs/02` §14 («المعالجة عبر الحدود تحتاج حسماً قانونياً»). يلزم تحقق فعلي من المناطق المتاحة، وقرار قانوني: نقطة أقرب منطقة متاحة + تقليل PII في الـ prompts + اتفاقية معالجة بيانات، أم تأجيل مرحلة الذكاء.
3. **العلاقة بين `USERS` و`EMPLOYEES`.** كيانان منفصلان في النموذج (مستخدمو البوابة ≠ سجلات HR) بينما بوابة الموظف تحتاج هوية دخول. أقترح: جدول `users` واحد للمصادقة، و`employees` سجل HR يرتبط به `user_id` اختياري.
4. **مراقبة الأخطاء و PDPL:** `docs/02` §3 يذكر Sentry، وحمولات الأخطاء قد تتضمن بيانات شخصية تخرج من المملكة (Sentry مستضاف خارجياً). البديل: GCP Error Reporting (يبقى في المشروع) أو Sentry self-hosted. يحتاج قراراً.
5. **مزوّد OTP** (مُعلَّق أصلاً في `docs/02` §14): Unifonic أم WhatsApp Business API الرسمي؟ يؤثر على المرحلة 1 (Auth).

### أسئلة قواعد عمل (أنفّذ الوثيقة حرفياً ما لم تقرر خلاف ذلك)

6. **مهلة الاعتراض 30 يوماً ثابتة لكل أنواع القضايا والمراحل؟** بعض المسارات نظاماً لها مهل أقصر (مثل المستعجلة). الوثيقة تنص على 30 ثابتة — أنفّذها كذلك، لكن أرفعها لك لأن «القواعد قانون».
7. **نهاية الخدمة:** معادلة `docs/06` هي الاستحقاق الكامل (حالة إنهاء من صاحب العمل). نظام العمل (م.85) يمنح المستقيل ثلثاً/ثلثين حسب المدة. هل ننفّذ المعادلة كما هي في v1 (مع حقل سبب انتهاء الخدمة مستقبلاً) أم ندعم الاستقالة من البداية؟
8. **GOSI:** النموذج يخزّن مبلغ الاشتراك يدوياً لكل موظف ولا توجد قاعدة نسب في الوثائق. هل يبقى حقلاً يدوياً في v1 أم نحسبه من النسب الرسمية (سعودي/غير سعودي)؟
9. **دقة تقريب الضريبة:** مع التخزين بالهللات، هل التقريب لأقرب هللة على مستوى **البند** أم **الفاتورة**؟ (زاتكا تعتمد منزلتين عشريتين — أقترح: على مستوى البند، والإجمالي جمع البنود.)
10. **نطاق الصفوف الافتراضي:** قيم النموذج الفعلية: شريك/محاسب/إداري/استقبال = `all`، محامي/مساعد = `assigned`. `docs/04` يشرح الآلية دون جدول القيم — أعتمد قيم النموذج هذه كافتراضيات؟
11. **مهمة تذكير الاعتراض:** النموذج ينشئها باستحقاق = المهلة − 5 أيام (غير موثّقة في `docs/06`). أعتمد الـ 5 أيام كقاعدة؟
12. **السعودة:** حساب النطاقات الفعلي في «قوى» يعتمد حجم المنشأة والنشاط؛ النموذج نسبة مبسّطة. المبسّط مقبول لـ v1؟

### ملاحظات تصميم من فحص كود النموذج (سأصححها في البناء — للعلم فقط)

- اكتشاف صدور الحكم يتم بـ **regex على نص نتيجة الجلسة** (`/حكم|النطق|نطقت|صدر الحكم/`) — في الإنتاج يكون حدثاً صريحاً (نوع جلسة «نطق بالحكم» + حقل تاريخ حكم)، لا مطابقة نصوص.
- `hrEos` يثبّت السنة الحالية 2026 وسنوات الخدمة تُحسب بفرق السنين فقط — الإنتاج يستخدم التاريخ الفعلي وكسور السنوات.
- `invPaid` يستنتج المدفوع من حقل الحالة عند غياب المدفوعات — الإنتاج: جدول مدفوعات والحالة مشتقة بالكامل.
- تعارض التقويم يقارن **التاريخ فقط** بلا وقت — جدول الجلسات في الإنتاج يتضمن وقتاً.
- إن غاب تاريخ الحكم تُحسب المهلة 30 يوماً من اليوم — سلوك احتياطي غريب في الإنتاج؛ الأصح إلزام إدخال تاريخ الحكم.

### تناقضات فعلية بين الوثائق والنموذج

لم أجد تناقضاً جوهرياً — الوثائق مستخرجة من النموذج بدقة عالية (تحققت من `PSEED`، `FIELD_DENY`، `CASE_SCOPE`، `NITAQAT_BANDS`، `_plus30`، `caseFeeNet`، `invStatusOf`، `caseConflicts`، `saudistsNeeded`، `advMonthlyOf/Remaining`، `hearingClash`، `mkInvoiceFromFee` — كلها مطابقة). ما وجدته فجوات توثيق صغيرة فقط (النقاط 10 و11 أعلاه، وصياغة أتعاب الساعة في `docs/06` التي تأكدت من معناها في الكود: القابلة للفوترة **وغير المفوترة بعد**).

**ملاحظة مستودع:** يوجد `index.html` في جذر المستودع (نسخة عرض مخففة، ليست من حزمة التسليم ولا تذكرها الوثائق). لا تتعارض مع شيء — فقط قرّر إن كانت تبقى (مثلاً لـ GitHub Pages) أم تُنقل/تُحذف.

---

## الخطوة التالية المقترحة

بعد اعتمادك هذه المراجعة (وحسم السؤالين 1 و5 على الأقل)، أبدأ **المرحلة 1**: تهيئة مشروع Next.js + TypeScript strict + Tailwind/shadcn RTL + i18n، مخطّط Prisma الأساسي (users/clients/cases + الصلاحيات + audit)، وAuth بـ OTP — دون المساس ببقية المراحل.
